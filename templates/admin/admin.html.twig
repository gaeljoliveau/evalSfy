<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Tito Salgado{% endblock %}</title>
    {{ encore_entry_link_tags('app') }}
</head>

<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" >
    <a class="navbar-brand" href="{{ url('homepage.index') }}">Accueil public</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ url('admin.artwork.index') }}">Oeuvres - admin</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('admin.exhibition.index') }}">Expositions - admin</a>
            </li>
        </ul>
    </div>
</nav>

<div style="margin-bottom: 100px"></div>



{% block content %}
{% endblock %}